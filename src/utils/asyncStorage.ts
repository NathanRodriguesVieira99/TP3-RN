// tentei e tentei usar o asyncStorge pro exercicio mas nao consegui

import AsyncStorage from '@react-native-async-storage/async-storage';

export interface CandidaturasProps {
  id: string;
  vaga: string;
  status: string;
  desc: string;
}

